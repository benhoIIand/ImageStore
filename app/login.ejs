<!DOCTYPE html>
<html lang="en" ng-app="samplr" class="page__login">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <title>Samplr</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="stylesheet" href="css/main.css">
    </head>
    <body>
        
        <div class="modal-overlay show clear">

            <div class="modal modal__login">

                <div class="modal__header">
                    <h3>Login</h3>
                </div>

                <div class="modal__body">

                    <!-- LOGIN FORM -->
                    <form action="/login?r=<%= referrer %>" method="post">
                        <div class="form-group">
                            <input type="text" class="input--full" name="email" placeholder="Email">
                        </div>
                        <div class="form-group">
                            <input type="password" class="input--full" name="password" placeholder="Password">
                        </div>

                        <button type="submit" class="btn btn-primary input--full">Login</button>
                    </form>

                    <!-- show any messages that come back with authentication -->
                    <% if (message.length > 0) { %>
                        <div class="bg-error"><%= message %></div>
                    <% } %>

                </div>

                <div class="modal__footer">
                    <p>Not go an account? <a href="/register">Register</a></p>
                    <p><a href="#">Forgotten your password?</a></p>
                </div>

            </div>

        </div>

    </body>
</html>
